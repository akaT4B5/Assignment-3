<%- include('../partials/header') %>

<div class="sessions-page-container">

    <div class="sessions-left-column">
        <h2>Sessions</h2>
        
        <a href="/sessions/add" class="card" id="add-session-btn">
            Add Session
        </a>

        <a href="/" class="btn-back-home">
            &larr; Back to Home
        </a>
    </div>
    <div class="sessions-right-column">
        
        <div class="scrollable-table-container">
            <table class="session-table">
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Focus</th>
                        <th>View Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (sessions.length > 0) { %>
                        <% sessions.forEach(session => { %>
                            <tr>
                                <td><%= new Date(session.date).toLocaleDateString() %></td>
                                <td><strong><%= session.focus %></strong></td>
                                <td>
                                    <details>
                                        <summary style="cursor: pointer; color: #007bff;">Show Workout</summary>
                                        <ul style="text-align: left; margin-top: 10px; font-size: 0.9em;">
                                            <% session.exercises.forEach(ex => { %>
                                                <li style="margin-bottom: 5px;">
                                                    <strong><%= ex.name %>:</strong>
                                                    <% if(ex.type === 'strength') { %>
                                                        <%= ex.sets %> x <%= ex.reps %> @ <%= ex.weight %>lbs
                                                    <% } else { %>
                                                        <%= ex.distance %>m in <%= ex.duration %> mins
                                                    <% } %>
                                                </li>
                                            <% }) %>
                                        </ul>
                                        <div style="margin-top: 10px;">
                                            <a href="/sessions/delete/<%= session._id %>" class="btn-delete" onclick="return confirm('Delete this entire session?')">Delete Session</a>
                                        </div>
                                    </details>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="3">No sessions logged yet.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        </div>
    </div> 
<%- include('../partials/footer') %>